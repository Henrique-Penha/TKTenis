'use client';

import styled from "styled-components";

const Header = () => {
    return (
        <>
            <StyledHeader>TKTenis</StyledHeader>
        </>
    )
}

const StyledHeader = styled.header`
    background-color: black;
    color: white;
`;

export default Header;